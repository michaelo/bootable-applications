#ifndef ECS_BASE_H
#define ECS_BASE_H

#include "inttypes.h"

typedef uint64_t EntityId; // Type for entity IDs
static const EntityId UndefinedEntity = 0;

typedef struct ECS ECS; //Forward declare

#endif
